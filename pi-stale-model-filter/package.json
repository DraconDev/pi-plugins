{
  "name": "pi-stale-model-filter",
  "version": "0.2.3",
  "type": "module",
  "description": "Hide superseded model versions across Pi model pickers, cycling, and configured scopes",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "typecheck": "npx -p typescript tsc --noEmit -p tsconfig.json"
  },
  "pi": {
    "extensions": ["extensions/stale-model-filter.ts"]
  }
}
