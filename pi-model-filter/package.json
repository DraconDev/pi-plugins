{
  "name": "pi-model-filter",
  "version": "0.1.0",
  "description": "Hide superseded version-suffixed models from pi's /model selector and Ctrl+P cycling. Keeps only the highest version per provider+base-name group.",
  "type": "module",
  "keywords": [
    "pi",
    "pi-package",
    "pi-extension",
    "model",
    "filter",
    "version",
    "selector"
  ],
  "license": "MIT",
  "files": [
    "extensions",
    "README.md"
  ],
  "scripts": {
    "test": "node --test tests/"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": [
      "./extensions"
    ]
  }
}
